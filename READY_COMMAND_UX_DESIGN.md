# Ready 命令用户体验设计

## 概述

重新设计 `gpf ready` 命令的用户体验，解决当前版本中的交互痛点，提供更智能、更便捷的分支就绪检查流程。

## 当前问题分析

### 用户痛点
1. **分支名输入困难**: `gpf ready xx/yy` 需要手动输入完整分支名，容易出错
2. **操作步骤繁琐**: 需要分别执行 `check`、`report`、`release`、`validate`，使用繁琐
3. **缺乏智能感知**: 命令无法根据当前目录上下文智能推断意图
4. **交互体验不佳**: 没有友好的选择界面

### 设计目标
- **零记忆负担**: 用户无需记住分支名称
- **一键完成**: 单次命令执行完整检查流程
- **智能上下文**: 根据当前目录智能推断操作对象
- **友好交互**: 提供直观的选择界面

## 新设计方案

### 命令使用方式

#### 方式一：无参数智能模式 (推荐)
```bash
gpf ready
```

**行为逻辑**：
1. **上下文检测**：
   - 在功能分支目录 (`.worktrees/xx--yy/`) → 直接检查当前分支
   - 在Epic目录 (`.worktrees/epic--xx/`) → 列出该Epic的所有功能分支供选择
   - 在项目根目录 → 列出所有功能分支供选择

2. **交互式选择**：
   - 使用方向键上下选择分支
   - 显示分支状态预览 (✅ 就绪 / ⚠️ 待处理 / ❌ 有问题)
   - 显示最后提交信息和时间

3. **自动执行流程**：
   - validate (验证依赖关系)
   - check (质量检查)  
   - report (生成报告)
   - release (准备发布，如果前面都通过)

#### 方式二：明确指定分支
```bash
gpf ready xx/yy
```

**行为逻辑**：
- 跳过交互选择，直接对指定分支执行完整流程

### 执行流程设计

#### 1. 上下文检测阶段
```bash
检测当前目录位置：
├─ 功能分支目录 (.worktrees/xx--yy/)
│  └─ 直接检查当前分支: xx/yy
├─ Epic目录 (.worktrees/epic--xx/)
│  └─ 列出Epic下的功能分支供选择
└─ 项目根目录
   └─ 列出所有功能分支供选择
```

#### 2. 分支选择界面 (仅无参数模式)
```
🚀 Ready 检查 - 选择要检查的功能分支

┌─ Epic: hooks-improvement ─────────────────────────────────┐
│ ✅ hooks-improvement/force-commit-support                 │
│    └─ 最后提交: 完善hooks改进文档 (2小时前)                │
│ ⚠️ hooks-improvement/documentation                        │
│    └─ 最后提交: 添加使用文档 (1天前)                      │
│ ❌ hooks-improvement/testing                              │
│    └─ 最后提交: WIP: 测试框架 (3天前)                     │
└──────────────────────────────────────────────────────────┘

使用 ↑↓ 选择分支，Enter 确认，Esc 退出
```

#### 3. 完整检查流程
按实践顺序自动执行：

1. **validate** - 验证依赖关系完整性
   - 检查基础分支同步状态
   - 验证分支间依赖关系
   - 检查外部依赖版本

2. **check** - 检查代码质量和状态
   - 功能分支状态检查
   - 工作目录清洁度检查
   - 代码质量检查
   - 测试覆盖率检查

3. **report** - 生成详细报告
   - 创建Markdown格式报告
   - 包含所有检查结果
   - 提供改进建议

4. **release** - 准备发布 (仅在前面都通过时)
   - 创建发布分支
   - 合并功能分支
   - 生成变更日志
   - 创建发布PR

### 状态显示设计

#### 分支状态图标
- ✅ **就绪**: 所有检查通过，可以发布
- ⚠️ **待处理**: 有警告但不阻塞发布
- ❌ **有问题**: 有错误需要解决
- 🔄 **检查中**: 正在执行检查
- 📋 **无提交**: 分支存在但没有提交

#### 实时进度显示
```
🔍 正在检查 hooks-improvement/force-commit-support...

✅ [1/4] 验证依赖关系完整性
🔄 [2/4] 检查代码质量和状态...
⏳ [3/4] 生成详细报告
⏳ [4/4] 准备发布
```

### 错误处理设计

#### 检查失败时的行为
```
❌ Ready 检查失败

问题总结:
  • 代码质量检查: 发现 2 个问题
  • 测试覆盖率: 低于要求阈值

详细信息:
  📄 完整报告: ./epic-hooks-improvement-readiness-report.md

建议操作:
  1. 修复代码质量问题
  2. 添加缺失的测试用例
  3. 重新运行: gpf ready

🔧 要强制继续吗? (y/N)
```

#### 用户选择处理
- **默认行为**: 停止执行，显示问题和建议
- **强制继续**: 用户确认后继续执行 release 步骤
- **详细报告**: 始终生成完整的 Markdown 报告

## 实现计划

### 阶段1: 上下文检测和智能选择 (2天)
- 实现目录上下文检测逻辑
- 实现分支列表生成和状态检测
- 实现交互式选择界面 (方向键选择)

### 阶段2: 流程整合和优化 (2天)  
- 重构现有的 check/report/release/validate 函数
- 实现自动顺序执行逻辑
- 优化错误处理和用户反馈

### 阶段3: 用户体验优化 (1天)
- 改进状态显示和进度指示
- 完善错误信息和建议
- 添加详细的帮助信息

## 技术实现要点

### 关键函数设计

```bash
# 主函数
cmd_ready() {
    local target_branch="$1"
    
    if [[ -z "$target_branch" ]]; then
        # 无参数模式：智能检测和选择
        target_branch=$(smart_branch_selection)
    fi
    
    # 执行完整检查流程
    execute_full_ready_pipeline "$target_branch"
}

# 智能分支选择
smart_branch_selection() {
    local context_type=$(detect_current_context_type)
    
    case "$context_type" in
        "feature_worktree")
            detect_current_feature_name
            ;;
        "epic_worktree")
            show_epic_branch_selection_menu
            ;;
        "project_root")
            show_all_branch_selection_menu
            ;;
    esac
}

# 完整检查流程
execute_full_ready_pipeline() {
    local branch="$1"
    
    ui_header "🚀 Ready 检查: $branch"
    
    # 按顺序执行检查
    validate_epic_dependencies "$branch" && \
    check_epic_readiness "$branch" && \
    generate_readiness_report "$branch" && \
    prepare_epic_release "$branch"
}
```

### UI组件需求
- 方向键选择菜单组件
- 进度条显示组件  
- 状态图标系统
- 实时状态更新机制

## 测试验证计划

### 功能测试
1. **上下文检测**: 在不同目录中测试命令行为
2. **分支选择**: 测试交互式选择的正确性
3. **流程执行**: 验证四个步骤的顺序执行
4. **错误处理**: 测试各种失败场景的处理

### 用户体验测试
1. **易用性**: 新用户是否能快速上手
2. **效率**: 与旧版本相比的操作效率
3. **清晰度**: 信息显示是否清晰易懂
4. **一致性**: 与其他命令的交互是否一致

## 成功标准

### 用户体验指标
- ✅ 分支选择时间减少 80% (从手动输入到选择)
- ✅ 操作步骤减少 75% (从4步到1步)  
- ✅ 用户错误率降低 90% (消除分支名输入错误)
- ✅ 用户满意度 > 4.5/5

### 技术指标
- ✅ 命令响应时间 < 2秒
- ✅ 上下文检测准确率 100%
- ✅ 向后兼容性 100% (支持原有语法)
- ✅ 测试覆盖率 > 90%

这个设计将显著提升 `gpf ready` 命令的用户体验，让开发者能够更高效地管理和检查功能分支的就绪状态。